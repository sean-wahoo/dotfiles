"use client";

const Page = (React.FC<{ children: React.ReactNode }> = ({ children }) => {
  const [assDick, setAssDick] = useState<boolean>();
  const ref = useRef<HTMLDivElement>(null);
  useEffect(() => {}, []);

  return <></>;
});
//"const [${1:state}, ${1:$1/^[a-z]/${$1:/upcase}/}] = useState<${2:string}>(${2:''});"
//
//
